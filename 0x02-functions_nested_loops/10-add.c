#include "holberton.h"
/**
 * add - Adds two integers.
 * @a: input number a.
 * @b: input number b.
 *
 * Return: Value of added numbers.
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
