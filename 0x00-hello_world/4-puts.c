#include <stdio.h>
/**
 * main - Prints str using puts
 * Description: Create char variable str
 * Outputs to stdout using puts then return
 * 0 when program ends.
 * Return: 0 when program ends.
 */
int main(void)
{
char str[] = "\"Programming is like building a multilingual puzzle";
puts(str);
return (0);
}
