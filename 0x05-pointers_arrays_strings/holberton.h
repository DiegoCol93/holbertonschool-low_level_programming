/* _putchar - Prints one char at a time. */
int _putchar(char c);

/* reset_to_98 - Updates the value to 98 of the pointed space in memory. */
void reset_to_98(int *n);

/* swap_int - Swaps the value of a and b. */
void swap_int(int *a, int *b);

/* _strlen - Returns the leng of a string. */
int _strlen(char *s);

/* _puts - Prints a string. */
void _puts(char *str);

/* print_rev - Prints a string reversed. */
void print_rev(char *s);

/* rev_string - Reverts a string. */
void rev_string(char *s);

/* puts2 - Prints an string oddly. */
void puts2(char *str);
